#!/bin/sh

set -eu

function download() {
  local name=$1
  local url=$2

  curl --location --silent --output _${name}.sh ${url}
  echo "✓ ${name}"
}

download mise https://mise.run
download rv https://github.com/spinel-coop/rv/releases/download/v0.2.0/rv-installer.sh
